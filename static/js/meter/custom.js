var ed_info = [
    [3, 'brooklyn', "At the house scene, Jane is thrown into a completely new situation and her thoughts on Ed are neutral."],
    [4, 'brooklyn', "Jane's impression of Ed improves vastly during the Sandwich scene when Ed opens up to her. She discovers that Ed is not like his wife and in the process gains more respect for him."],
    [4, 'office', "Although Jane (nearly) consummates her relationship with Ed at this point, she still keeps being in Beth's study in the back of her mind."],
    [2, 'hunter', "Jane's respect for Ed nearly disintegrates during this scene in front of Devon's school. This devolves into an argument between the two."],
    [3, 'train', "Jane's respect for Ed returns to a neutral position as she breaks up her relationship with Ed."]
];

var beth_info = [
    [3, 'brooklyn'],
    [3, 'supermarket'],
    [2, 'office'],
    [1, 'brooklyn'],
    [5, 'train']
];

var sang_info = [
    [4, 'train'],
    [2, 'brooklyn'],
    [3, 'airport'],
    [1, 'train'],
    [1, 'train']
];

var nina_info = [
    [3, 'brooklyn'],
    [4, 'brooklyn'],
    [1, 'airport'],
    [2, 'train'],
    [2, 'train']
];

var devon_info = [
    [1, 'brooklyn'],
    [2, 'ctown'],
    [3, 'brooklyn'],
    [4, 'hunter'],
    [3, 'train']
];

/*
var ed_bgchange = function() {
    $("header.masthead").css('background', 'linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url("../img/bgimg/' + ed_slider + '")');
};

var ed_slider = ed_info[$("#ed-slider").slider().on('slide', ed_bgchange).data('slider')][1];

var beth_bgchange = function() {
    $("header.masthead").css('background', 'linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url("../img/bgimg/' + beth_slider + '")');
};

var beth_slider = beth_info[$("#beth-slider").slider().on('slide', beth_bgchange).data('slider')][1];

var sang_bgchange = function() {
    $("header.masthead").css('background', 'linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url("../img/bgimg/' + sang_slider + '")');
};

var sang_slider = sang_info[$("#sang-slider").slider().on('slide', sang_bgchange).data('slider')][1];

var nina_bgchange = function() {
    $("header.masthead").css('background', 'linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url("../img/bgimg/' + nina_slider + '")');
};

var nina_slider = nina_info[$("#nina-slider").slider().on('slide', nina_bgchange).data('slider')][1];

var devon_bgchange = function() {
    $("header.masthead").css('background', 'linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url("../img/bgimg/' + devon_slider + '")');
};

var devon_slider = devon_info[$("#devon-slider").slider().on('slide', devon_bgchange).data('slider')][1];
*/